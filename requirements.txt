# BUITEMS Copilot — dependencies (pinned for reproducible, auditable installs).
# Install with:  python -m pip install -r requirements.txt
#
# Versions verified free of known vulnerabilities via pip-audit (see SECURITY_AUDIT.md).
# Pin these so every machine (and BUITEMS's deployment) runs the exact same,
# audited versions — and so a future audit knows exactly what to check.

# --- Web server / core ---
Flask==3.1.3
python-dotenv==1.2.2

# --- LLM (Groq / Llama) for natural phrasing + Roman Urdu ---
groq==1.6.0

# --- Charts (GPA trend image) ---
matplotlib==3.11.1

# --- Document Q&A / RAG (general policy questions only; no student data) ---
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2
faiss-cpu==1.15.0
sentence-transformers==5.7.0
